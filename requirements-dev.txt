# Dev dependencies for factory import pitch normalization and CI tests.
# Install: pip install -r requirements-dev.txt
numpy>=1.24
scipy>=1.10
librosa>=0.10

# Optional (higher-quality pitch shift): brew install rubberband  /  apt install rubberband-cli
